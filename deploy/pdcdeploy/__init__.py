"""PDC <-> Fluxx deployment tooling.

Read-only status reporting for now: compare what is deployed in a Fluxx
instance against the source of truth in this repository.

Stdlib only, by design. The eventual operators of this tool are not all
developers, and a `pip install` step is a place deployments go to die.
"""

__version__ = "0.1.0"
